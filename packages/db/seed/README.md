# Seed data

Empty by design (matches the original scaffold). Add minimal per-environment
seed data here once a service exists that needs it to run locally — e.g. a
default tenant + admin user for control-plane-api local development.
Nothing here yet in Phase 1: only the schema and role exist so far, no
service reads or writes through it.
