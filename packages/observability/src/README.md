# Observability Package
Correlation-ID propagation, structured logging, and the agent-specific trace
events required by clause 55 (model routing alternatives considered/rejected,
policy decisions) — plain latency/error metrics are not sufficient here.
