# Agent Registry (your actual agents go here)
One sub-folder per agent you define, each starting from ../_template/agent_specification_template_v1.4.md.
No agent here should be specific to any single downstream product (clause 75) —
that decision belongs at the project_registry / project level, not in the
core agent set.
