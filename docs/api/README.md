# API contract

The actual OpenAPI source of truth lives at
`services/control-plane-api/openapi/control_plane_openapi_v1.4.yaml` (kept
next to the service that implements it, per the original scaffold layout).
This folder is a pointer, not a duplicate — do not copy the spec here.
