# Model Router & Provider Gateway
Capability-based routing (clause 10), fallback chain with bounded backoff
(clause 49), writes every decision to routing_decision_records (clause 72).
Add one adapter per provider under adapters/.
